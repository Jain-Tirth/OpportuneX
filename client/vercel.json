{
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://unistop-backend.vercel.app/api/:path*"
    }
  ],
  "ignoreCommand": "git diff --quiet HEAD^ HEAD -- . ':!../README.md' ':!../.gitignore' ':!../render.yaml' ':!../*.md'"
}
